name: test
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Verify bundled data channel
        run: scripts/verify-xcframework.sh
      - name: Run tests
        run: swift test
